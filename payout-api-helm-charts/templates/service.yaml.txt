apiVersion: v1
kind: Service
metadata:
  name: {{ include "payout-api.fullname" . }}
  labels:
    app: payout-api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: payout-api